# Uncomment the next line to define a global platform for your project
# platform :ios, '18.0'

target 'expense-tracker-mobile' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for expense-tracker-mobile
  pod 'SwiftKeychainWrapper'

  target 'expense-tracker-mobileTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'expense-tracker-mobileUITests' do
    # Pods for testing
  end

end
